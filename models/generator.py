import torch
from torch import tensor, nn, optim
import torch.nn.functional as F

class CNNGen(nn.Module):
    def __init__(self, )


    raw_data_fake, batch_fake = self.generator.generate(batch_size, i)

    def generate(self, batch_size, mode):
